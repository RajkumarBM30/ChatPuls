services:
  - type: web
    name: chatplus
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_GENERATIVE_AI_API_KEY
        sync: false
    autoDeploy: true